<Page
    x:Class="Talkift.Client.Views.V2.MessageList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Talkift.Client.Models.V2"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid>
        <ListView x:Name="MessageListView" Padding="16,8"
                  SelectionMode="None"
                  ItemsSource="{x:Bind ViewModel.FilteredMessages}"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  ItemsUpdatingScrollMode="KeepLastItemInView">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:ChatMessageModel">
                    <Border Padding="4,2">
                        <TextBlock Text="{x:Bind Content}" TextWrapping="Wrap" FontSize="14"/>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock x:Name="EmptyText" Text="No messages found."
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                   Visibility="Collapsed"/>

        <TextBlock x:Name="LoadingText" Text="Loading..."
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontSize="14" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                   Visibility="Collapsed"/>
    </Grid>
</Page>
