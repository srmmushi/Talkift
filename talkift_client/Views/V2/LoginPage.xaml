<Page
    x:Class="Talkift.Client.Views.V2.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:Talkift.Client.Views"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" VerticalAlignment="Center" Spacing="16">
            <TextBlock Text="Welcome to Talkift" FontSize="32" FontWeight="Bold"
                       HorizontalAlignment="Center" Margin="0,40"/>
            <FontIcon Glyph="&#xE716;" FontSize="72" HorizontalAlignment="Center"
                      Margin="0,0,0,20"/>
        </StackPanel>

        <Grid Grid.Row="1" HorizontalAlignment="Center" Width="360">
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="12" Padding="24">
                <StackPanel Spacing="16">
                    <StackPanel Spacing="6">
                        <TextBlock Text="Username" FontSize="14" FontWeight="SemiBold"/>
                        <TextBox x:Name="UsernameBox"
                                 Text="{x:Bind ViewModel.Username, Mode=TwoWay}"
                                 PlaceholderText="Enter username" CornerRadius="8" Padding="12,8"/>
                    </StackPanel>

                    <StackPanel Spacing="6">
                        <TextBlock Text="Password" FontSize="14" FontWeight="SemiBold"/>
                        <PasswordBox x:Name="PasswordBox"
                                     Password="{x:Bind ViewModel.Password, Mode=TwoWay}"
                                     PlaceholderText="Enter password" CornerRadius="8" Padding="12,8"/>
                    </StackPanel>

                    <Button x:Name="LoginButton" Style="{StaticResource AccentButtonStyle}"
                            Content="Login" Click="LoginButton_Click"
                            HorizontalAlignment="Stretch" CornerRadius="8" Padding="16,8"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="16">
                        <TextBlock Text="Don't have an account?"
                                   Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                        <HyperlinkButton Content="Register" Click="RegisterHyperlink_Click"/>
                    </StackPanel>

                    <Button x:Name="OfflineLoginButton" Style="{StaticResource IconButtonStyle}"
                            Content="Offline Login" Click="OfflineLoginButton_Click"
                            HorizontalAlignment="Stretch" CornerRadius="8" Padding="12,8"
                            Foreground="{ThemeResource SystemAccentColor}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
