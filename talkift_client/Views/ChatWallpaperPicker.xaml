<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="Talkift.Client.Views.ChatWallpaperPicker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="DialogRoot"
    Title="Chat Wallpaper"
    PrimaryButtonText="Apply"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    Width="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="ChooseText" Text="Choose a wallpaper"
                   FontSize="14" Margin="0,0,0,12"/>

        <GridView Grid.Row="1" x:Name="WallpaperGridView"
                  SelectionMode="Single" Padding="4">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <Border Width="120" Height="80" CornerRadius="8"
                            Background="{Binding}" Margin="4"/>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8"
                    HorizontalAlignment="Center" Margin="0,12,0,0">
            <TextBlock x:Name="CustomText" Text="Or use custom color:"
                       VerticalAlignment="Center" FontSize="12"/>
            <ColorPicker x:Name="CustomColorPicker" Width="40" Height="32"/>
        </StackPanel>
    </Grid>
</ContentDialog>
