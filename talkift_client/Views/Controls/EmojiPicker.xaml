<UserControl
    x:Class="Talkift.Client.Views.Controls.EmojiPicker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Width="320" Height="380"
    Background="Transparent">

    <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
          BorderThickness="1" CornerRadius="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Emoji" FontSize="14" FontWeight="SemiBold" Padding="12,8"/>

        <TextBox Grid.Row="1" x:Name="SearchBox"
                 PlaceholderText="Search emoji..." CornerRadius="4" Padding="8,4" Margin="8,0,8,4"/>

        <GridView Grid.Row="2" x:Name="EmojiGridView"
                  Padding="8" SelectionMode="Single" ItemClick="EmojiGridView_ItemClick">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" FontSize="24" Padding="4"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</UserControl>
