<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Talkift.Client.Views.VoiceRecorder"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Visibility="Collapsed">

    <Grid Padding="12,8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
          BorderThickness="0,1,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <ProgressRing Grid.Column="0" x:Name="RecordingIndicator"
                      Width="20" Height="20" IsActive="True"
                      Foreground="Red" Margin="0,0,8,0"/>

        <TextBlock Grid.Column="1" x:Name="RecordingTimeText" Text="00:00"
                   FontSize="14" FontWeight="SemiBold"
                   VerticalAlignment="Center"/>

        <Button Grid.Column="2" x:Name="CancelButton"
                Style="{StaticResource IconButtonStyle}"
                ToolTipService.ToolTip="Cancel" Click="CancelButton_Click">
            <FontIcon Glyph="&#xE711;" FontSize="16"/>
        </Button>

        <Button Grid.Column="3" x:Name="SendButton"
                Style="{StaticResource AccentButtonStyle}"
                ToolTipService.ToolTip="Send" Click="SendButton_Click">
            <FontIcon Glyph="&#xE724;" FontSize="16"/>
        </Button>
    </Grid>
</UserControl>
