<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Talkift.Client.Views.ServerListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:Talkift.Client.Views"
    xmlns:Models="using:Talkift.Client.Models"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="20,12" HorizontalAlignment="Left">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <TextBlock x:Name="HeaderText"
                       Text="Servers"
                       FontSize="20" FontWeight="SemiBold"
                       VerticalAlignment="Center"/>
        </Grid>

        <InfoBar x:Name="StatusInfoBar"
                 Grid.Row="1"
                 IsClosable="True"
                 IsOpen="False"
                 Margin="0,0,0,10"
                 HorizontalAlignment="Stretch"/>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ListView x:Name="ServerListViewCtrl"
                      ItemsSource="{x:Bind ViewModel.Servers}"
                      SelectionMode="Single"
                      IsItemClickEnabled="True"
                      ItemClick="ServerListView_ItemClick"
                      SelectionChanged="ServerListView_SelectionChanged">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="Models:Server">
                        <Grid Padding="12,10" CornerRadius="8"
                              Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                              BorderThickness="1"
                              Margin="0,0,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Grid Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0">
                                <Ellipse Width="36" Height="36"
                                         Fill="{ThemeResource SystemControlBackgroundAccentBrush}"/>
                                <TextBlock Text="{x:Bind local:ServerListView.GetInitial(Name)}"
                                           FontSize="14" FontWeight="SemiBold"
                                           Foreground="White"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                            </Grid>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{x:Bind Name}"
                                           FontSize="13" FontWeight="SemiBold"/>
                                <TextBlock Text="{x:Bind local:ServerListView.GetAddressDisplay(Address, Port)}"
                                           FontSize="11"
                                           Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                           Margin="0,1,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" VerticalAlignment="Center" Orientation="Horizontal" Spacing="4">
                                <Ellipse Width="8" Height="8"
                                         VerticalAlignment="Center"
                                         Fill="{x:Bind local:ServerListView.GetStatusBrush(IsOnline)}"/>
                                <TextBlock FontSize="11"
                                           Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                           Text="{x:Bind local:ServerListView.GetStatusText(IsOnline)}"
                                           VerticalAlignment="Center"/>
                            </StackPanel>

                            <Grid.ContextFlyout>
                                <MenuFlyout>
                                    <MenuFlyoutItem Text="{x:Bind local:ServerListView.Localized('EditServer')}" Click="EditServer_Click">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon Glyph="&#xE70F;"/>
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                    <MenuFlyoutSeparator/>
                                    <MenuFlyoutItem Text="{x:Bind local:ServerListView.Localized('Delete')}" Click="DeleteServer_Click"
                                                     Foreground="Red">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon Glyph="&#xE74D;" Foreground="Red"/>
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                </MenuFlyout>
                            </Grid.ContextFlyout>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <TextBlock Grid.Row="0"
                       x:Name="EmptyStateText"
                       Text="No servers added yet."
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="14"
                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                       Visibility="Collapsed"/>
        </Grid>
    </Grid>
</Page>
