<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Talkift.Client.Views.ConversationListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Talkift.Client.Models"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8">
                <FontIcon Glyph="&#xE716;" FontSize="20" VerticalAlignment="Center"/>
                <TextBlock x:Name="HeaderText" Text="Conversations"
                           Style="{StaticResource SubtitleTextBlockStyle}"
                           VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="4">
                <Button x:Name="CreateGroupButton"
                        Click="CreateGroupButton_Click"
                        Padding="8,4"
                        CornerRadius="4">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <FontIcon Glyph="&#xE710;" FontSize="14"/>
                        <TextBlock x:Name="GroupButtonText" Text="Group" FontSize="12"/>
                    </StackPanel>
                </Button>
                <Button x:Name="CreateConvButton"
                        Click="CreateConvButton_Click"
                        Padding="8,4"
                        CornerRadius="4">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <FontIcon Glyph="&#xE715;" FontSize="14"/>
                        <TextBlock x:Name="ChatButtonText" Text="Chat" FontSize="12"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <InfoBar Grid.Row="1"
                 x:Name="StatusInfoBar"
                 Severity="Informational"
                 IsOpen="False"
                 IsClosable="True"
                 Margin="0,0,0,8"/>

        <ListView Grid.Row="2"
                  x:Name="ConversationListView"
                  IsItemClickEnabled="True"
                  SelectionMode="None"
                  ItemClick="ConversationListView_ItemClick"
                  ItemsSource="{x:Bind ViewModel.Conversations}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Conversation">
                    <Grid Padding="12,10" CornerRadius="8"
                          Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                          BorderThickness="1"
                          Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Grid Grid.Column="0" Width="36" Height="36"
                              VerticalAlignment="Center">
                            <Ellipse Fill="{ThemeResource AccentColor}"/>
                            <FontIcon Glyph="&#xE716;"
                                      FontSize="16"
                                      Foreground="White"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </Grid>

                        <StackPanel Grid.Column="1" Margin="12,0,0,0"
                                    VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind Name}"
                                       FontWeight="SemiBold"
                                       FontSize="14"
                                       TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{x:Bind LastMessage}"
                                       FontSize="12"
                                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                       TextTrimming="CharacterEllipsis"
                                       Margin="0,2,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" VerticalAlignment="Top"
                                    HorizontalAlignment="Right" Spacing="4">
                            <TextBlock Text="{x:Bind LastMessageTime}"
                                       FontSize="11"
                                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                            <Border x:Name="UnreadBadge"
                                    Background="#E81123"
                                    CornerRadius="10"
                                    Padding="6,2"
                                    HorizontalAlignment="Right"
                                    Visibility="Collapsed">
                                <TextBlock Text="{x:Bind UnreadCount}"
                                           Foreground="White"
                                           FontSize="11"
                                           FontWeight="SemiBold"/>
                            </Border>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock Grid.Row="2"
                   x:Name="EmptyText"
                   Text="No conversations yet."
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                   Visibility="Collapsed"/>
    </Grid>
</Page>
